#!/bin/bash

LIBDIR='/usr/lib/manjaro-tools'

[[ -r ${LIBDIR}/util-msg.sh ]] && source ${LIBDIR}/util-msg.sh

for f in $(find . -maxdepth 1 -name '*.iso' | cut -d'/' -f2); do
  msg2 "creating checksums for [$f]"
  md5sum $f > $f.md5
  sha1sum $f > $f.sha1
  signfile $f
done
